---
const { class: className, ...rest } = Astro.props;
---

<figure {...rest} class:list={["mdx-table w-full overflow-x-auto my-8 rounded-xl border border-border dark:border-border-dark", className]}>
  <table class="w-full text-sm text-left">
    <slot />
  </table>
</figure>
